{
  "name": "email-viewer-monorepo",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "cd api && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "node api/index.js",
    "dev": "concurrently \"PORT=3001 node api/index.js\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.3.0"
  }
}
